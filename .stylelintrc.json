{
  "extends": ["stylelint-config-standard-scss"],
  "rules": {
    "scss/at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["theme", "layer", "apply", "variants", "responsive", "screen", "utility", "utilities"]
      }
    ]
  }
}
